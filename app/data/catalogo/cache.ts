/** Etiqueta compartida por las lecturas públicas y las escrituras del panel. */
export const CATALOG_CACHE_TAG = "catalogo";

/** Máximo de una hora si una invalidación del panel no llegara a ejecutarse. */
export const CATALOG_REVALIDATE_SECONDS = 3600;
